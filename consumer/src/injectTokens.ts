export enum COMMON_PATHS {
    apiV1 = '/api/v1',
    book = '/books',
}

export enum COMMON_IDENTIFIERS {
    IBookCommandService = 'IBookCommandService',
    IBookQueryService = 'IBookQueryService',
    ILogger = 'ILogger',
    IConfig = 'IConfig',
}

export enum COMMON_REPOSITORIES {
    IBookRepository = 'IBookRepository',
}

export enum COMMON_MODELS {
}
